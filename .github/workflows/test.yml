name: Test

on:
  push:
    branches:
      - main

jobs:
    test:
        runs-on: ubuntu-latest
        environment:
            name: production
        steps:
            - name: Checkout
              uses: actions/checkout@v4
            
            - name: Echo secrets
              run: |
                echo ${{ secrets.REPO_SECRET }}
                echo ${{ secrets.SECRET_KEY }}