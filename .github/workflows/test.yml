name: Test

on:
  push:
    branches:
      - main

jobs:
    test:
        runs-on: ubuntu-latest
        environment:
            name: maintenance
        steps:
            - name: Checkout
              uses: actions/checkout@v4
            
            - name: Echo secrets
              run: |
                echo ${{ secrets.MESSAGE }} | sed 's/./& /g'